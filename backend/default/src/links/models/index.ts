// Junction models for the links in this directory. They are generated from the
// `defineLink(...)` definitions (see ../index.ts), and re-exported here so the
// ORM tooling discovers a conventional `models/` directory for the link module
// (e.g. `damat-orm generate:types link`).
export { models } from "../index";

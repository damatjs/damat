[install]

# Don't install packages published in the last 2 days
minimumReleaseAge = 172800

# Block packages that use exotic lifecycle scripts
# (native binaries / suspicious install behavior)
enableScripts = false

# Require explicit approval for packages that need build scripts
optional = true

[install.scopes]



# @damatjs/load-env — release notes

Change history for this package. For how it works **now**, read the
[package README](../../packages/core/env/README.md) and its
[docs](../../packages/core/env/docs/).

| Version | Summary                                                                                                                                                            | Upgrade notes         |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- |
| 0.1.3   | Maintenance / repo-wide version bump — the changeset describes cross-module links (`@damatjs/link`); no change to this package's own code (it has no dependencies) | —                     |
| 0.1.2   | Maintenance / repo-wide version bump — the changeset describes relation-by-table-name in the ORM packages; no change to this package's own code                    | —                     |
| 0.1.1   | Maintenance — CI and test cleanup                                                                                                                                  | —                     |
| 0.1.0   | First published minor release — zero-dependency `.env` cascade loader; merges all matching files into `process.env`, system env wins                               | [0.1.0 →](./0.1.0.md) |
| 0.0.10  | Pre-release — build fix (`tsc-alias` for `@/` path aliases); optional inherited package config                                                                     | —                     |
| 0.0.9   | Pre-release — build fix (`tsc-alias` module resolution in published packages)                                                                                      | —                     |
| 0.0.8   | Pre-release — CI build fix for nested packages; `prepublishOnly` check kept                                                                                        | —                     |
| 0.0.7   | Pre-release — build-error fix; version sync to 0.0.7                                                                                                               | —                     |
| 0.0.6   | Pre-release — include `dist` in published package; add `prepublishOnly` guard                                                                                      | —                     |
| 0.0.5   | Pre-release — build fixes                                                                                                                                          | —                     |
| 0.0.4   | Pre-release — build fixes                                                                                                                                          | —                     |
| 0.0.3   | Pre-release — build fixes                                                                                                                                          | —                     |
| 0.0.2   | First pre-alpha release — core features functional end-to-end (not production-ready)                                                                               | —                     |

# @damatjs/deps — release notes

Change history for this package. For how it works **now**, read the
[package README](../../packages/deps/README.md) and its
[docs](../../packages/deps/docs/).

| Version | Summary | Upgrade notes |
|---------|---------|---------------|
| 0.1.3 | Maintenance / version-sync bump — cross-module links land in other packages (`@damatjs/link`); deps' own re-export surface and pins are unchanged | — |
| 0.1.2 | Maintenance / version-sync bump — relation-by-table-name lands in the ORM packages; deps' own surface and pins are unchanged | — |
| 0.1.1 | Maintenance / version-sync bump — CI and test cleanup; no change to deps' own surface | — |
| 0.1.0 | First published minor release — pinned, curated re-exports of the shared third-party deps (`hono`, `zod`, `effect`, `pg`, `ioredis`, `nanoid`, `uuid`) via per-library subpaths plus a namespaced root | [0.1.0 →](./0.1.0.md) |
| 0.0.10 | Maintenance / version-sync bump — `tsc-alias` for `@/` path resolution in published builds; optional inherited package config | — |
| 0.0.9 | Maintenance / version-sync bump — `tsc-alias` for `@/` module resolution in published packages | — |
| 0.0.8 | Maintenance / version-sync bump — CI builds nested packages; `prepublishOnly` check kept | — |
| 0.0.7 | Maintenance / version-sync bump — build-error fix; version sync | — |
| 0.0.6 | Maintenance / version-sync bump — include `dist` in published package; add `prepublishOnly` guard | — |
| 0.0.5 | Maintenance / version-sync bump — build fixes | — |
| 0.0.4 | Maintenance / version-sync bump — build fixes | — |
| 0.0.3 | Maintenance / version-sync bump — build fixes | — |
| 0.0.2 | First published pre-alpha — core features functional end-to-end (not production-ready) | — |
</content>
</invoke>

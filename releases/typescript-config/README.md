# @damatjs/typescript-config — release notes

Change history for this package. For how it works **now**, read the
[package README](../../packages/typescript-config/README.md) and its
[docs](../../packages/typescript-config/docs/).

This is a **private, never-published** package (`"private": true`, version
`0.0.0`). It is not part of the `0.1.x` line that the publishable packages
follow — it has no published versions and no `CHANGELOG.md`. The single
baseline file below describes what the shared TypeScript presets provide.

| Version | Summary | Upgrade notes |
|---------|---------|---------------|
| 0.0.0 | Baseline — shared `base.json` preset plus `nextjs.json` and `react-library.json` frontend presets | [0.0.0 →](./0.0.0.md) |

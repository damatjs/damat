# @damatjs/logger 1.0.3

## Fixed

Pretty error output no longer prints a separate name/message line immediately
before a stack whose first line contains the same text. Errors without a stack
still render their normalized name and message.

# @damatjs/schema-codegen 1.0.3

## Fixed

- A `belongsTo` relation now uses its declared model property when the
  foreign-key-derived field name collides with a concrete column.
- Generated interfaces keep both the scalar foreign key and the distinct
  optional loaded relation instead of emitting duplicate property names.

No model source changes are required. Regenerate affected schema types.

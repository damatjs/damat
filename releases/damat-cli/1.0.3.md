# @damatjs/damat-cli 1.0.3

## Fixed

- Packed Damat packages are validated to use one exact coordinated internal
  dependency graph.
- The release gate installs packed tarballs into a clean consumer and verifies
  the CLI, standalone module PTY lifecycle, MCP lookup, and framework singleton.
- Verbose module failures contain one handled summary followed by one stack.

Upgrade all `@damatjs/*` packages together to 1.0.3. Do not mix this release
with the incorrectly pinned internal dependency graph published in 1.0.2.

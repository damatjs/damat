# @damatjs/orm-type Unreleased

## Changed

`OrmModule` may carry resolved entry, model, and migration paths plus package
mutability and package-name metadata.

## Action required

Existing descriptors remain valid because every new field is optional.
